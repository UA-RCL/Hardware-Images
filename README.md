# Hardware Images

This repository may contain multiple hardware image/project folders.

To use a specific image, switch to that image's folder and follow the README inside that folder.

Example:

```bash
cd <image_folder>
```

Then follow the build, load, or programming instructions provided there.