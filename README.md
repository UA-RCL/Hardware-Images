# General Information:

- This repository contains example HLS accelerators and build code targeting Alveo boards (currently only U280). 
- Each folder contains HLS code for specific accelerator design with build commands. Each accelerator only supports certain parameters, which can be changed according to the needs of your target application. 
- Currently supported accelerators are:
    - Conv2D: 
        - Kernel size: 3x3
        - Stride = 1
        - Dilation = 1
        - Padding = 0
    - GEMM:
        - Matrix A size: 512x512
        - Matrix B size: 512x512
    - ZIP:
        - Support all sizes
        - ADD, SUB, MULT, DIV are supported
    - VADD:
        - Basic vector addition (already covered by ZIP-ADD)

    - Conv2D: 
        - Kernel size: 3
        - Stride = 1
        - Dilation = 1
        - Padding = 1

- Firstly, all the accelerators are packaged into IPs, and then the build flow performs synthesis, P&R on the FPGA board.

- Keep in mind that accelerators in this repository are still under development and requires rigorous testing. 

# Build Instructions:

- Install or source Vitis 2023.2 (vitis version must match xrt version for the most headache-free experience)
- Build flow should be pretty stable and follow the instructions below for building binary for Alveo. 

## Single Accelerator Build Flow:

- Each folder has a Makefile that builds an xo (AMD object file) file. This object contains the packaged IP generated by Vitis compiler. It will be utilized for the generation of final binary that's programmed into Alveo board. Run following commands for the build:

```bash
cd <accelerator_source_code>
make xo
```

- Let's say you would like to build xo file for ZIP, run the following commands:

```bash
cd ZIP
make xo
```

- If you would like to add a new design, please create a new folder and place the source code. Then copy the Makefile to the folder and change the following lines:

```Makefile
KERNEL_SRC_CPP := <cpp_files>
$(BUILD_DIR)/<accelerator_name>.xo: ${KERNEL_SRC_CPP}
xo: $(BUILD_DIR)/<accelerator_name>.xo
```

- If you face any errors at this step, then it means your code has problems ;)

## Final Binary Generation

- You can pack multiple kernels into a single binary file and instantiate it within CEDR. First, modify the ```krnl_build_options.cfg``` file by defining number of desired accelearator instances. You can also do cool stuff like setting specific HBM or DRAM banks here but afaik Vitis compiler infers them automatically. Below example is from ZIP accelerator:

```bash
make xclbin
```

```Makefile
[connectivity]
nk=zip:1                <number of instances>
sp=zip_1.input1:HBM[0]  <Input 1 is connected to HBM bank 0>
sp=zip_1.input2:HBM[1]  <Input 2 is connected to HBM bank 1>
sp=zip_1.output:HBM[2]  <Output is connected to HBM bank 2>
```

- The function name here should match the top function name in your design. Say you would like to add a Conv2D instance, then cfg file should look like below:

```Makefile
[connectivity]
nk=zip:1                
sp=zip_1.input1:HBM[0]  
sp=zip_1.input2:HBM[1]  
sp=zip_1.output:HBM[2]  

nk=Filter2DKernel:1     
```

- The rest is going to be synthesis and P&R, which will be handled by the Vitis automatically. First add the xo file to the Makefile in the root folder:

```Makefile
v++ $(VPPFLAGS) --config $(KERNEL_CONFIG_FILE) -l -o $@ <add all the xo files here>
```

- Then run the following command:
```bash
make xclbin
```

- Once this process finishes, you can just grab the xclbin file and program it into the board using CEDR. (That tutorial will be in somewhere else)

Any bugs fix it yourself, any questions ask to chatgpt or deepseek-R1 or github-copilot. Or you can just ask to suluhan@arizona.edu.
